
            // Text('Step Count Overview', style: Theme.of(context).textTheme.headline6),

            // Bar Chart for Step Counts
            // SizedBox(height: 8),
            // SizedBox(
            //   // height: 200,
            //   child: StepCountBar(leftValue: widget.measurement.leftSteps, rightValue: widget.measurement.rightSteps,)
            // ),
